// ThrottleManager.h stub for native unit tests.
// MomentumController forward-declares ThrottleManager but never calls it directly,
// so all we need is the type definition.
#pragma once

class ThrottleManager {};
